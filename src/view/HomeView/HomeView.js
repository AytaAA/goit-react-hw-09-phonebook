import React from "react";
import styles from "../HomeView/HomeView.module.css";

const HomeView = () => (
  <div className={styles.container}>
    <h1 className={styles.title}>Manage your contacts with ease </h1>
  </div>
);

export default HomeView;
